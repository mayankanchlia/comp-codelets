#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main(){
	int t ;
	cin >> t;
	for (int i = 0 ; i <t ; i++){
		int n ; 
		cin >> n;
		int arr[n];
		int count;
		for (int j = 0 ; j < n;j++){
			cin >> arr[j];
			
		}
		sort(arr,arr+n);
		//cout << GetLargestSum(arr,n);
		for(int j = 1 ; j<n-1;j++){
			

		}
		// cout << count;
	}

}